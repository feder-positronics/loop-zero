# Python

Apply only when selected and the task touches Python. Resolve the environment,
dependency tool and owning test conventions from local instructions and
`workflow.toml`; do not create a competing environment. Identify whether the
owning test needs external resources before using a broader suite.
