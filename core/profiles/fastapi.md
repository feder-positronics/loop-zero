# FastAPI

Apply only when selected and the task touches FastAPI. Resolve local API,
persistence and authorization contracts before changing a boundary. Identify
which owning checks require database or service isolation and which schema or
client artifacts the repository requires after an API change. No authentication
scheme, ORM or generation command is supplied by this profile.
