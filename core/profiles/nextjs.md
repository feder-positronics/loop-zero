# Next.js

Apply only when selected and the task touches Next.js. Resolve the repository's
installed version, routing and server/client rendering boundaries before editing.
Use local component and UI evidence conventions and owning checks; a compilation
pass alone does not establish changed interaction behavior.
