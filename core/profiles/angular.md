# Angular

Apply only when selected and the task touches Angular. Resolve local component,
dependency injection and state conventions against the installed version. Select
the owning component/service or UI checks from the repository's existing runners;
do not import another consumer's rendering or component conventions.
