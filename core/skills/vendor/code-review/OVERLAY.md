## loop-zero overlay

The vendored text is the base. These rules win on conflict; `core/CONTRACT.md`
wins over both.

Use the consumer's configured review budget and finding schema. A review never
grants mutation, commit, publication, or merge authority.
